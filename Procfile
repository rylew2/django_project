# My Heroku File
# web is the process type - this process will receive web traffic when deployed
# gunicorn - command to run web process - way for python to talk to web server - through wsgi file
# <myproject.wsgi>

web: gunicorn django_project.wsgi